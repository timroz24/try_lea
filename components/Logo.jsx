import React from 'react';

const Logo = () => {
    return (
        <div className="absolute top-6 left-6 bg-black p-2 rounded-lg z-10">
            {
            /*
            <img
                src="/lea-logo.svg"
                alt="LEA Logo"
                className="w-12 h-12 md:w-16 md:h-16"
                onError={(e) => {
                    // Fallback to text if image fails to load
                    e.target.style.display = 'none';
                    e.target.nextSibling.style.display = 'block';
                }}
            />
            <div
                className="w-12 h-12 md:w-16 md:h-16 bg-gradient-to-r from-purple-600 to-orange-500 rounded-lg flex items-center justify-center text-white font-bold text-lg md:text-xl hidden"
                style={{ display: 'none' }}
            >
                LEA
            </div>
            */}

        </div>
    );
};

export default Logo; 